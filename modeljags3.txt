
model {
# Priors
alpha ~ dnorm(40.71,pow( 2.65,-2))
beta ~ dnorm(-0.69, pow( 0.28,-2))
beta2 ~ dnorm(0, 0.0001)
beta3 ~ dnorm(0, 0.0001)
sigma ~ dunif(0, 50) # May consider smaller range
tau <- pow(sigma, -2)
# Likelihood
for (i in 1:n) {
  y[i] ~ dnorm(mu[i], tau) # Response y distributed normally
  mu[i] <- alpha + beta * x[i] + beta2*x2[i] + beta3*x[i]*x2[i] # Linear predictor
}
}
